// __DESIGN_COMPANION_DEV_ONLY__
// Post-MVP scaffolding — not yet implemented. See spec §10.3 for the
// archive policy (bundle older `applied/` intents into quarter archives
// after 100 files or 3 months). The npm script entry exists so
// package.json stays consistent with the rest of the design:* family;
// running it is a no-op printout until the post-MVP iteration fills it
// in.
console.log('design:archive — post-MVP scaffolding; not yet implemented. See spec §10.3.');
process.exit(0);
